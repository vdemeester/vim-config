" force terminal to use 256 colors
set t_Co=256
if has("gui_running")
	set guifont=Monospace\ 9
endif

" Vinnsento is a macbook, using bepo most of the time..
" I currently don't know how to detect the keymap "name"
" Let's force the keymap to bepo for now on
let g:keymap_bepo = 1
exe 'source $HOME/.vim/vimrc-bepo'
